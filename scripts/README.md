# Scripts

GDScript files grouped by responsibility.

- `autoload/`: global managers registered as autoloads.
- `components/`: reusable behavior attached to scene nodes.
- `systems/`: game rules and coordination logic.
